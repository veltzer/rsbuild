[build]
# Number of parallel jobs (1 = sequential, 0 = auto-detect CPU cores)
parallel = 1

[processors]
enabled = ["template", "lint"]

[lint]
linter = "ruff"
args = []

[completions]
shells = ["bash"]
